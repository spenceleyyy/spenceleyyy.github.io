{
  "name": "travel-buddy",
  "version": "0.1.0",
  "private": true,
  "description": "Travel Buddy - iOS wrapper for the translator UI",
  "scripts": {
    "cap:init": "cap init \"Travel Buddy\" com.icantspeakit.app",
    "cap:add-ios": "cap add ios",
    "cap:sync": "cap sync ios",
    "cap:open": "cap open ios"
  },
  "dependencies": {
    "@capacitor/core": "^6.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^6.0.0",
    "@capacitor/ios": "^6.0.0"
  }
}
